
class Invoice {
    constructor() {
    }

    create(params: {
        recipients: Array<{ address: string, amount: number }>,
        currency: string,
        chain: string,
           }) {
        
    }
}